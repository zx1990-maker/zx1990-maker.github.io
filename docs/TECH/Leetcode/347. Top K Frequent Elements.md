# 347. Top K Frequent Elements

**link:** https://leetcode.com/problems/top-k-frequent-elements/description/

## Description:

Given an integer array nums and an integer k, return the k most frequent elements. You may return the answer in any order.

<img width="615" height="345" alt="image" src="https://github.com/user-attachments/assets/055962d0-1ca0-45e4-a0a1-918e2a030521" />

## Solution:

```Python
class Solution(object):
    def topKFrequent(self, nums, k):
        # 1. 哈希表统计频次
        memo = {}
        for num in nums:
            memo[num] = memo.get(num, 0) + 1
            
        # 2. 创建桶数组，长度为 len(nums) + 1
        # 为什么加1？因为一个数字最多出现 len(nums) 次，我们要保证索引够用
        buckets = [[] for _ in range(len(nums) + 1)]
        
        # 3. 将数字放入对应的频次桶中
        for num, freq in memo.items():
            buckets[freq].append(num)
            
        # 4. 倒序遍历桶，收集前 K 个高频元素
        res = []
        for i in range(len(buckets) - 1, 0, -1):
            if buckets[i]: # 如果这个桶里有数字
                res.extend(buckets[i]) # 把桶里的数字全部加入结果
            if len(res) >= k:
                return res[:k] # 收集够了 K 个，直接返回
                
        return res
```
        
